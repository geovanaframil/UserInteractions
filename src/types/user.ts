export type User = {
  id: number
  name: string
  email: string
  phone: string
  city: string
}

export type UserFormValues = {
  name: string
  email: string
  phone: string
  city: string
}

export type UserInput = UserFormValues

